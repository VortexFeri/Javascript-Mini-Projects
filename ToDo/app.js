function listItem(text, importance, status) {
    this.text = text;
    this.importance = importance;
    this.status = status;
  }

function addNew(text, importance, status) {
    listItem(text, importance, status)
}

function removeItem() {
    
}